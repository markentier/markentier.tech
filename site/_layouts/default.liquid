<!DOCTYPE html>
<html lang="en">
  <head>
    {% include "head.liquid" %}
  </head>
  <body>
    <div id="wrppr">
      <header>
        {% include "header.liquid" %}
      </header>
      <nav>
        {% include "nav.liquid" %}
      </nav>
      <main id="content">
        <article>
          {% if page.title != "home" %}<h1>{{ page.title }}</h1>{% endif %}
          {{ page.content }}
        </article>
      </main>
      <footer>
        {% include "footer.liquid" %}
      </footer>
    </div>
  </body>
</html>
