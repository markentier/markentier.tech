<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
<!-- build at: {{config.build_timestamp}} -->
{%- for page in pages | sort(attribute="date") | reverse %}
{%- set is_post = page.permalink is containing("/posts/") %}
{%- if is_post == true %}
  <url>
    <loc>{{page.permalink | safe}}</loc>
    {% if page.date %}<lastmod>{{page.date | date}}</lastmod>{% endif %}
    <priority>0.85</priority>
  </url>
{%- endif %}
{%- endfor %}
{%- for tax in taxonomies %}
    {%- for item in tax %}
      {%- set is_tag = item.permalink is containing("/tags/") %}
      {%- if is_tag == true %}
  <url>
    <loc>{{item.permalink | safe}}</loc>
    <priority>0.3</priority>
  </url>
      {%- endif %}
    {%- endfor %}
  {%- endfor %}
  <url>
    <loc>{{config.base_url | safe}}/</loc>
    <changefreq>weekly</changefreq>
    <priority>0.9</priority>
  </url>
</urlset>
